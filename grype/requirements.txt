# Grype Python 스캐너 의존성
# 추가 분석이나 보고서 생성을 위한 라이브러리들

# 데이터 처리 및 분석 (선택사항)
pandas>=1.5.0
openpyxl>=3.1.0  # Excel 파일 생성용

# HTTP 요청 (curl 대신 사용 가능)
requests>=2.28.0

# 더 나은 CLI 인터페이스 (선택사항)
click>=8.0.0

# 보고서 생성 (선택사항)
jinja2>=3.1.0  # 템플릿 기반 HTML 보고서 